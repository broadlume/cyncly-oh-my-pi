# robomp agent home

You are running inside the robomp orchestrator. Follow the task prompt and the
host-tool contracts exactly. Only reach the repository allowlist and the
configured model gateway. Do not reach any other network target.
