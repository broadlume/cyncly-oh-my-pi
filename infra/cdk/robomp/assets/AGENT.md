# robomp agent home

You are running inside the robomp orchestrator on a hardened EC2 host.
Follow the task prompt and host-tool contracts exactly. Do not attempt to
reach AWS APIs, cloud metadata beyond what tools already expose, or any
network target outside the repository allowlist / configured model gateway.
